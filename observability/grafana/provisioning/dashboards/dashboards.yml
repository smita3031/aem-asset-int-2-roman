apiVersion: 1

providers:
  - name: Roman Numeral Service
    orgId: 1
    folder: Roman Numeral Service
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
